{
  "buildCommand": "cd ../.. && pnpm install && cd reelshort-web && npx nuxt build",
  "installCommand": "cd ../.. && pnpm install",
  "outputDirectory": ".output",
  "framework": "nuxtjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/server/index.mjs" }
  ]
}
